# ── HLTV API v4.1 Environment Configuration ──
# Copy to .env.local for local development

# Backend API URL (proxied through Next.js rewrites)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Theme defaults
NEXT_PUBLIC_DEFAULT_THEME=dark

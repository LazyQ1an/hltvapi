# HLTV API v5.0 - Single-IP Cloudflare Bypass
# ============================================================

# -- Core ------------------------------------
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
pyyaml>=6.0

# -- HTTP & Anti-Bot -------------------------
httpx[http2]>=0.27,<1.0
h2>=4.1,<5.0
curl-cffi>=0.6,<1.0
# playwright is optional -- install only if stealth mode is needed
# pip install playwright && playwright install chromium

# -- HTML Parsing ----------------------------
beautifulsoup4>=4.12,<5.0
lxml>=5.1,<6.0
selectolax>=0.3,<1.0

# -- Cache -----------------------------------
diskcache>=5.6,<6.0
cachetools>=5.3,<6.0

# -- CLI -------------------------------------
typer>=0.9,<1.0

# -- API Server ------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# -- Logging ---------------------------------
structlog>=24.1,<25.0

# -- Scheduler -------------------------------
apscheduler>=3.10,<4.0
psutil>=5.9,<7.0

# -- Monitoring & Observability --------------
prometheus-client>=0.19,<1.0
opentelemetry-api>=1.22,<2.0
opentelemetry-sdk>=1.22,<2.0
opentelemetry-exporter-otlp>=1.22,<2.0
opentelemetry-instrumentation-fastapi>=0.43b0

# -- Security --------------------------------
slowapi>=0.1.9
python-jose[cryptography]>=3.3

# -- Export Center ---------------------------
reportlab>=4.0,<5.0
openpyxl>=3.1,<4.0

# -- i18n ------------------------------------
babel>=2.14,<3.0

# -- Test ------------------------------------
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
